def selection_sort(vals):

    n = len(vals)
    for i in range(0, n):
        for j in range(i+1, n):
            if vals[j] < vals[i]:
                vals[i], vals[j] = vals[j], vals[i]
    return vals

d = [2, 4, 5, 1, 3, 6, 9, 8, 7]
print(selection_sort(d))
